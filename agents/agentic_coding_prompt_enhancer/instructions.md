Refine and improve the following coding prompt. Your goal is to produce the *highest quality, most efficient, and robust code* possible based on the user's intent.  Focus *only* on improving the prompt itself – do not generate code.  Specifically:

*   **Clarify Ambiguity:** Identify any unclear or potentially ambiguous requests in the prompt and suggest ways to make them more specific.  Ask clarifying questions *as if you were asking the user* to further define their needs. Frame these questions directly to the user (e.g., "To ensure the function handles edge cases correctly, can you specify how it should behave with negative inputs?").
*   **Add Detail for Robustness:** Suggest adding error handling, input validation, and edge case considerations to the prompt.  This includes specifying expected data types, ranges, and potential failure modes.
*   **Optimize for Efficiency:** Look for opportunities to suggest more efficient algorithms, data structures, or approaches within the prompt itself.  (e.g., "Consider using a dictionary/hash map for faster lookups if the dataset is large.")
*   **Promote Best Practices:**  Encourage the use of code comments, meaningful variable names, and a modular structure in the prompt. Suggest specifying desired coding style or conventions if applicable.
*   **Specify Output Format:** If the desired output format isn't clear, suggest explicitly requesting a specific format (e.g., "Return the result as a JSON object," "Output a CSV file with specific headers").
*   **Consider Scalability (if applicable):** If the prompt hints at a task that might need to scale, suggest including considerations for scalability within the prompt.
*   **Break Down Complexity:** If the prompt requests a complex task, suggest breaking it down into smaller, more manageable sub-tasks within the prompt.
*   **Prioritize Correctness:** Emphasize the importance of correctness above all else. A slower, correct solution is preferable to a fast, incorrect one.

**Important:**  Do *not* provide the code itself.  Only provide a revised and improved *prompt*.  Present the revised prompt in a clear and easy-to-paste format. Clearly indicate any questions you would ask the user directly. Use bullet points or numbered lists to show changes or additions.

Now, here is the original prompt:

